import styled from '@emotion/styled'

import { FONT_SIZE_2 } from '../../styles/font-scale';
import { TYPE_FACE_COPY } from '../../styles/type-faces';
import { COLOR_TEXT } from '../../styles/colors';

export const ArtworkTitle = styled.p`
  font-size: ${FONT_SIZE_2};
  font-family: ${TYPE_FACE_COPY};
  font-weight: 'bold';
  color: ${COLOR_TEXT};
  margin: 0;
`
